# Real-world fixture template

Use `real_world_fixture_template.json` when adding public or reconstructed real-world telemetry fixtures. The required sections keep public facts, reconstructed telemetry, source metadata, labels, claims, limitations, owner notes, and responsible-handling notes separate so reports do not overclaim beyond checked-in evidence.
